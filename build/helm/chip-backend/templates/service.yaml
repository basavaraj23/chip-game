apiVersion: v1
kind: Service
metadata:
  name: chip-backend
spec:
  selector:
    app: chip-backend
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: ClusterIP
